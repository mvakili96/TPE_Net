_1 means that all YDHR data are added to training, so that validation set be the same as previous trainings in which we only used Railsem for trining and validation.

withYDHR without _1 means that YDHR are randomly added to training and validation